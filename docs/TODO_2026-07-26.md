# To-do memo — 2026-07-26

*State at the end of the session in which we cleared the INPI licence question, built the dry-run
guard, and researched the FR/GB/Infogreffe signup paths.*
*Sorted by what blocks what, not by size.*

---

## 0. Waiting on someone else

| # | Item | Waiting on | Note |
|---|---|---|---|
| 0.1 | **INPI API access** | INPI | Request submitted 2026-07-26. Registration alone is not enough — the live login returned `connection_type_not_allowed` / "Accès impossible pour API". When the answer comes, check whether INPI issues **separate technical identifiers**; if so those, not the portal login, go into `.env` |
| 0.2 | **Companies House API key** | you | `GB_COMPANIES_HOUSE_API_KEY` is still empty in `.env`. Create **two** applications — test and live — they have separate keys |
| 0.3 | **KYC.com reply** | Colin / Latika | **SENT 2026-07-27.** Asks for the ordering process, per-document prices for the four additional docs (DK/BE/HK/IL), a correction of our tier split for the 7 sandbox-only jurisdictions, and shareholder-data fill rates |
| 0.4 | **Denmark S2S credentials** | you, then Erhvervsstyrelsen | Gmail draft `r-8111797625774233102` to `cvrselvbetjening@erst.dk` — **written, not sent.** Fill in the annual volume first. It also asks the two open DK questions: are regnskaber retrievable via the API, and are vedtægter available at all |

---

## 1. France — the moment INPI access lands

1.1 **Live-validate the `# SECONDARY` markers in `inpi_client.py`.** This is the first thing to do and
the reason the client has never been trusted: the `/companies/{siren}/attachments` response field
names and the `/actes/{id}/download` + `/bilans/{id}/download` paths come from a third-party
implementation and have never seen a real response. Test target ready: **SEPTEO, SIREN 790675037**.
Note it is an **SAS**, so its statuts will not evidence shareholders — fine for validating the
download path, useless as a shareholder test.

1.2 **Also validate the best-effort name search** (`search()` with a non-SIREN query). Currently
marked unverified; if it does not work, callers must always pass a SIREN.

1.3 **Implement art. 2.4 attribution as a data field, not a template string.** Source *and the date of
last update* must be captured **at fetch time** per record. This cannot be reconstructed afterwards —
if we skip it now, FR attribution becomes impossible without re-fetching everything.

1.4 **Phase 3 — map into the BetterCo app:** statuts → `ARTICLES_OF_ASSOCIATION`, comptes → a
'Sonstige' document (`OTHERKYCDOCS_*`). FR has no Gesellschafterliste for SAS/SA — nothing to file
there.

1.5 **Art. 2.5 is a product constraint, not a client detail.** No search by natural-person criteria
over RNE data (art. A.123-69 code de commerce). **This binds the search widget**, not just this
integration. Whoever builds the widget needs to know before designing the search form.

---

## 2. France — Infogreffe (the paid half). Decide before spending

2.1 **Answer the blocking question first: can documents be ordered via API at all, or is it
portal-only?** Nothing on the Infogreffe pages establishes a programmatic ordering endpoint. The one
government document API (API Entreprise "Extrait RCS", Infogreffe-sourced) is restricted to public
administrations — **not available to us**. This decides what Phase 4 even is: a client, or a human
process with a queue.

2.2 **Only then decide on the 85,00 €HT/year subscription** (first user; 42,50 € each additional, max
5). Committing to an annual fee before knowing whether it can be automated is the wrong order.

2.3 **Do not build an Infogreffe client yet.** None exists — the only grep hit in the repo is the
`KbisViaInfogreffe` guard message. That guard works correctly and makes no network call.

2.4 **Evaluate separately: DataInfogreffe "Associés, actionnaires", 3,00 €HT per call, self-service
API.** Identification of shareholders plus distribution of shares. Our current position is that the
French shareholder list is `not_provable_from_registry` for SAS/SA and only available as KYC.com
data. This is a cheap direct alternative. It is **data, not a proof document** — same category as the
KYC.com `shareholders` fields — but it changes the sourcing options and deserves its own decision.

2.5 **Keep the split-vendor routing as designed.** Infogreffe charges **7,26 € TTC for actes et
statuts** — the documents INPI serves free. Routing statuts to INPI and only the Kbis (2,55 €HT /
3,06 € TTC incl. electronic transmission) to Infogreffe saves 7,26 € per company.

---

## 3. UK — the moment the key lands

3.1 **Build the Companies House client**, mirroring the `inpi_client.py` shape so it drops into the
same router: `search`/company lookup → `filing-history` → `GET /document/{id}/content`.

3.2 **Backoff from day one.** 600 requests / 5 minutes, then `429` for the rest of the window;
repeated violations can suspend the account.

3.3 **Mind the main correctness trap:** filing history is a *chronology*, not a current-state view.
"The articles" means the **latest amending filing**, not the first match. Getting this wrong produces
a plausible, wrong document.

3.4 **Do not classify CS01 as a shareholder list.** It is a periodic delta. This is already our
documented position; the client must not quietly contradict it.

3.5 **Definition of done** is the 7-point list in `DIRECT_INTEGRATION_START_2026-07-26.md` §7 —
ending with the measured cost delta versus the vendor path, which is the number that justifies DK and
FR.

---

## 4. Cross-cutting

4.1 **Provenance on every stored document** — source, retrieval date, cost. Needed for the FR
attribution obligation (1.3) *and* for the cost reporting the whole least-cost thesis rests on.

4.2 **Fallback, not replacement.** If a direct call fails, the router falls back to the vendor. Direct
integration lowers cost; it must not lower availability.

4.3 **Fix a naming inconsistency I introduced and have not yet fixed:**
`DIRECT_INTEGRATION_START_2026-07-26.md` proposes `FR_INPI_USERNAME`, but `inpi_client.py` reads
`INPI_USERNAME`. The doc follows the code, not the other way round.

4.4 **Tier B/C/D stay parked.** IT and ES are worth costing only once Tier A ships (IT needs
SPID/CIE/CNS + prepaid Telemaco credit; ES's CORPME channel is for very-high-volume integrators
only). **KW has no registry intelligence record at all** — either research it or accept vendor-only
permanently.

---

## 5. Done this session, for the record

- **INPI licence question resolved** — the full 2024 licence text says documents *are* in scope
  (art. 1) and redistribution *is* permitted (art. 2.2). `FR_INPI_INTEGRATION.md` §6 corrected; it
  had wrongly named the generic "Licence Ouverte" as the instrument.
- **Dry-run built into `inpi_client.py`** — hard network guard (`DryRunBlocked`), a `plan` command,
  a `--dry-run` flag, and 7 new self-test checks including tripwires on `session.request/post` that
  prove nothing leaves the process. Self-test 17/17.
- **Signup paths researched and written up** — `SIGNUP_FR_GB_2026-07-26.md`, now covering INPI,
  Companies House **and** both Infogreffe products.
- **INPI project description drafted** in French and English — `INPI_PROJECT_DESCRIPTION_FR.md`,
  structured along the licence articles. Submitted.
- **KYC.com email drafted** with the 15-jurisdiction table — Gmail draft, not sent.
